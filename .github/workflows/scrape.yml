name: Satta King Scraper

on:
  schedule:
    # Run every 25 mins between 4:00 AM and 6:30 AM IST (22:00 - 01:00 UTC)
    - cron: '*/25 22,23,0,1 * * *'
    # Run every 25 mins between 4:00 PM and 11:30 PM IST (10:30 - 18:00 UTC)
    - cron: '*/25 10-18 * * *'
  workflow_dispatch: # Allow manual triggering

permissions:
  contents: write
  deployments: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15 # Reduced timeout since we aren't waiting 5 mins inside the script anymore
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 📦 Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        
    - name: 📥 Install dependencies
      run: |
        cd scraper
        npm install --no-package-lock
        
    - name: 🚀 Run scraper
      run: |
        cd scraper
        node scraper.js
      env:
        NODE_ENV: production
        
    - name: 🗺️ Generate Sitemap
      run: |
        cd scraper
        node generate-sitemap.js
        
    - name: 📊 Commit and push results
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add public/data/results.json public/data/historical/ public/sitemap.xml public/data/manifest.json
        git commit -m "🤖 Auto-update results - $(date -u '+%Y-%m-%d %H:%M:%S UTC')" || exit 0
        git push

    - name: 🚀 Deploy to Cloudflare Pages
      uses: cloudflare/pages-action@v1
      with:
        apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        projectName: sattaking786
        directory: public
        gitHubToken: ${{ secrets.GITHUB_TOKEN }}
